# Aegis_GRC_PLATFORM

AWS/EC2-ზე deploy/update ერთი ბრძანებით:

```bash
bash deploy/deploy.sh
```

პირველ გაშვებამდე აუცილებელია:

```bash
cp backend/.env.production backend/.env
nano backend/.env
```
